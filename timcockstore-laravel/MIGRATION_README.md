# TimCockStore - Миграция на Laravel

Проект успешно перенесён с Vanilla PHP на Laravel! 🎉

## 📋 Что было перенесено

✅ **Все основные функции:**
- Аутентификация пользователей (вход, регистрация, выход)
- Каталог товаров с фильтрацией по категориям
- Корзина покупок (хранится в сессии)
- Оформление заказов
- Личный кабинет с историей заказов
- Система ролей (client, support, manager)
- Панели управления для менеджера и поддержки

✅ **Структура БД:**
- Таблица `users` (с добавлением поля `role`)
- Таблица `categories` (категории товаров)
- Таблица `products` (товары с привязкой к категориям)
- Таблица `orders` (заказы пользователей)
- Таблица `order_items` (содержимое заказов)

✅ **Статические файлы:**
- CSS стили (скопированы в `public/css`)
- Изображения товаров (скопированы в `public/img`)

## 🚀 Запуск проекта

### Предварительные требования
- PHP 8.1+
- Composer
- MySQL 8.0
- Git (опционально)

### Шаги для запуска

1. **Перейти в директорию проекта:**
```bash
cd D:\Developing\Programming\(767)\timcockstore\timcockstore-laravel
```

2. **Установить зависимости (если не установлены):**
```bash
composer install
```

3. **Запустить миграции:**
```bash
php artisan migrate
```

4. **Заполнить БД тестовыми данными:**
```bash
php artisan db:seed
```

5. **Запустить dev сервер:**
```bash
php artisan serve
```

6. **Открыть в браузере:**
```
http://localhost:8000
```

## 👤 Тестовые учётные записи

После запуска seeder'а доступны следующие тестовые учётные записи:

| Email | Пароль | Роль |
|-------|--------|------|
| manager@example.com | password | Менеджер |
| support@example.com | password | Поддержка |
| client@example.com | password | Клиент |

## 🏗️ Структура проекта

```
timcockstore-laravel/
├── app/
│   ├── Http/Controllers/
│   │   ├── HomeController.php         # Главная страница
│   │   ├── AuthController.php         # Аутентификация
│   │   ├── CatalogController.php      # Каталог товаров
│   │   ├── CartController.php         # Корзина
│   │   ├── OrderController.php        # Заказы
│   │   └── ProfileController.php      # Профиль пользователя
│   └── Models/
│       ├── User.php
│       ├── Product.php
│       ├── Category.php
│       ├── Order.php
│       └── OrderItem.php
├── database/
│   ├── migrations/                    # Миграции БД
│   └── seeders/
│       └── DatabaseSeeder.php         # Заполнение тестовыми данными
├── resources/
│   └── views/
│       ├── layout.blade.php           # Базовый шаблон
│       ├── index.blade.php            # Главная страница
│       ├── catalog.blade.php          # Каталог
│       ├── product.blade.php          # Страница товара
│       ├── cart.blade.php             # Корзина
│       ├── checkout.blade.php         # Оформление заказа
│       ├── order-confirmation.blade.php # Подтверждение заказа
│       ├── profile.blade.php          # Личный кабинет
│       ├── auth/
│       │   ├── login.blade.php        # Вход
│       │   └── register.blade.php     # Регистрация
│       ├── manager/
│       │   └── dashboard.blade.php    # Панель менеджера
│       └── support/
│           └── dashboard.blade.php    # Панель поддержки
├── public/
│   ├── css/                           # CSS стили
│   └── img/                           # Изображения товаров
├── routes/
│   └── web.php                        # Маршруты приложения
└── .env                               # Переменные окружения
```

## 📝 Основные маршруты

| URL | Описание |
|-----|----------|
| `/` | Главная страница |
| `/login` | Вход в аккаунт |
| `/register` | Регистрация |
| `/catalog` | Каталог товаров |
| `/product/{id}` | Страница товара |
| `/cart` | Корзина покупок |
| `/checkout` | Оформление заказа (требует авторизации) |
| `/profile` | Личный кабинет (требует авторизации) |

## 🔧 Полезные команды

```bash
# Откат миграций и повторное создание с заполнением данными
php artisan migrate:refresh --seed

# Просмотр списка всех маршрутов
php artisan route:list

# Создание администратора
php artisan tinker
>>> User::create(['name' => 'Admin', 'email' => 'admin@example.com', 'password' => Hash::make('password'), 'role' => 'manager'])

# Просмотр логов
tail -f storage/logs/laravel.log
```

## 🐛 Возможные проблемы и решения

### Проблема: "Application key missing"
**Решение:**
```bash
php artisan key:generate
```

### Проблема: "Base table or view not found"
**Решение:** Убедитесь, что миграции выполнены:
```bash
php artisan migrate
```

### Проблема: "Column not found" при seed'е
**Решение:** Откатите и заново запустите миграции:
```bash
php artisan migrate:refresh --seed
```

### Проблема: Статические файлы не загружаются
**Убедитесь, что:**
1. CSS файлы скопированы в `public/css/`
2. Изображения скопированы в `public/img/`
3. В браузере очищен кэш (Ctrl+Shift+Del)

## 📚 Дополнительные ресурсы

- [Laravel документация](https://laravel.com/docs)
- [Laravel Blade шаблонизатор](https://laravel.com/docs/blade)
- [Eloquent ORM](https://laravel.com/docs/eloquent)

## ✨ Что дальше?

Вы можете:
1. Добавить более сложные фильтры в каталог
2. Реализовать систему рейтинга товаров
3. Добавить email уведомления при создании заказа
4. Настроить платёжные системы (Stripe, Yandex.Kassa и т.д.)
5. Добавить API для мобильного приложения
6. Внедрить систему логирования и мониторинга

---

**Проект успешно перенесён на Laravel!** 🚀
Для вопросов и поддержки обратитесь к документации Laravel.

